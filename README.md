# Password-Generator
This is a simple Password Generator program written in C++. It generates a random password of user-defined length using a mix of uppercase letters, lowercase letters, and numbers.  The program uses random number generation (rand() and srand(time(0))) to ensure that each password is unique every time the program runs.
